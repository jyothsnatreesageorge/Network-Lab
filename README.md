1. Implement client to server communication using socket programming with TCP as protocol.   
2. Implement bi-directional chat using socket programming with TCP as protocol.   
3. Implement a multi-user chat server using TCP as transport layer protocol.   
4. Implement bi-directional client-server communication using socket programming with UDP as protocol.   
5. Implement a Concurrent Time Server application using UDP to execute the program at a remote server. Client
sends a time request to the server, server sends its system time back to the client. Client displays the result.   
6. Implement Stop-and-Wait ARQ flow control protocol.   
7. Implement Selective Repeat ARQ flow control protocol.   
8. Implement Distance Vector Routing algorithm.   
9. Implement Link State Routing algorithm.   
10. Develop a concurrent file server which will provide the file requested by a client if it exists. If not, the server
sends appropriate message to the client. Server should also send its process ID (PID) to clients for display along
with the file or the message.   
11. Implement leaky bucket algorithm for congestion control.   
12. Implement Go-Back-N (GBN) ARQ flow control protocol.   
13. Write a UDP-based Client-Server program where the Client reads and sends a number to the Server. The Server
must check if the number is Prime or Composite; if Prime, it returns the number’s factors to the Client for
display.   
14. Write a TCP-based Client-Server program where the Client reads and sends a number to the Server. The Server
calculates the factorial of that number and sends the result back to the Client to be displayed.   